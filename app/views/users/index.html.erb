<% provide(:title, 'All RoboUsers') %>

<h1> All Available RoboUsers </h1>

<ul class = "users">
	<% @users.each do |user| %>
		<l1>
			<%= gravatar_for user, size:50 %>
			<%= link_to user.name, user %>
		</li>
	<% end %>

</ul>